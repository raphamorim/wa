// // Creates an Vec of elements split into groups the length of size. If array can't be split evenly, the final chunk will be the remaining elements.
// #[inline]
// pub fn chunk<T>(arr: Vec<T>, size: usize) -> Vec<T> {
//     vec![]
// }

// #[test]
// fn test_chunk() {
//     let a = chunk(vec!['a', 'b', 'c', 'd'], 2); // => [['a', 'b'], ['c', 'd']]

//     let b = chunk(vec!['a', 'b', 'c', 'd'], 3); // => [['a', 'b', 'c'], ['d']]

//     assert_eq!(a, vec![vec!['a', 'b'], vec!['c', 'd']]);
//     assert_eq!(b, vec![vec!['a', 'b', 'c'], vec!['d']]);
// }
